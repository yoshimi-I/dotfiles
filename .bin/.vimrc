" jjでインサートモードを抜ける設定
inoremap jk <Esc>

" ヤンクでクリップボードに保存
set clipboard=unnamedplus

let mapleader = "\<space>"

xnoremap p pgvy

" leader + a で行の先頭（最初の非空白文字）に移動
nnoremap <leader>a ^

" leader + e で行の末尾に移動
nnoremap <leader>e $
